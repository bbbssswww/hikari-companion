# 参与贡献

感谢你愿意帮助 Hikari 成长。项目欢迎代码、设计、角色格式、语音、测试、文档和产品讨论。

## 开始之前

1. 阅读 README、路线图和架构说明。
2. 在 Issue 中确认问题尚未有人处理；较大的功能请先讨论方案。
3. 从 `main` 创建简短、聚焦的功能分支。
4. 不要提交 API 密钥、私人对话、未授权角色素材或生成服务的付费凭证。

## 本地开发

```bash
pnpm install
pnpm dev
```

提交前运行：

```bash
pnpm typecheck
pnpm test
pnpm build
```

## Pull Request 要求

- 一个 PR 解决一个清晰问题。
- 说明用户能观察到的变化和验证方式。
- 界面修改附截图或短视频。
- 新能力说明需要哪些权限、数据流向哪里、如何撤销。
- 新角色或素材说明来源与许可证。
- 破坏兼容性的修改必须先提交设计讨论。

## 代码约定

- TypeScript 使用严格模式，不用 `any` 绕过关键数据类型。
- 渲染进程不直接访问 Node.js；通过受限的 preload API 调用桌面能力。
- API 密钥不得进入日志、错误信息或前端持久化。
- 危险操作必须经过统一权限层，不能在插件中自行跳过确认。
- 新功能应尽量附带单元测试或明确的手动验证步骤。

## 适合第一次贡献的方向

查看 [docs/initial-issues.md](./docs/initial-issues.md) 中标记为 `good first issue` 的任务。提交前可先在对应 Issue 留言，避免重复工作。
